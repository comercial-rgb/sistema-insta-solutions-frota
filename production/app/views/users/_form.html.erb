<%= simple_form_for @user, validate: false do |f| %>

	<!-- ID do perfil de usuário -->
	<%= f.input :profile_id, as: :hidden, input_html: {value: @user.profile_id} %>

	<% render 'users/forms/profile_image', f: f %>
	
	<%= render 'users/forms/personal_data', f: f %>

	<% render 'users/forms/person_type_form', f: f %>
	
	<% render 'common_pages/attachments/attachment', f: f %>

	<% render 'common_pages/attachments/attachments', f: f %>
	
	<% render 'common_pages/addresses/address', relation: :address, f: f %>
	
	<% render 'common_pages/addresses/addresses', f: f %>
	
	<% render 'common_pages/phones/phones', f: f %>
	
	<% render 'common_pages/emails/emails', f: f %>
	
	<% render 'common_pages/data_banks/data_banks', f: f %>
	
	<% render 'common_pages/data_banks/data_bank', f: f %>
	
	<% render 'common_pages/cards/cards', f: f %>

	<% render 'users/forms/manually_plan', f: f %>

	<% render 'common_pages/person_contacts/person_contacts', f: f %>

	<%= render 'common_pages/save', f: f, return_path: (@current_user.id != @user.id) ? users_user_path : nil %>

<% end %>

<%
# End of file _form.html.erb
# Path: ./app/views/users/_form.html.erb
%>
