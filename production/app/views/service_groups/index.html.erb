<%
breadcrumbs = [
	{active: false, href: root_path, text: t('menu.initial_page')},
	{active: false, href: service_groups_path, text: ServiceGroup.model_name.human(count: 2)},
	{active: true, href: '#', text: t('model.list')},
]
%>

<%= 
	render 'common_pages/index',
	attributes: {
		page_title: ServiceGroup.model_name.human(count: 2),
		url_new_path: new_service_group_path,
		policy_true: policy(ServiceGroup).new?,
		objects: @service_groups,
		objects_path: service_groups_path,
		object_name: ServiceGroup.model_name.human,
		breadcrumbs: breadcrumbs,
		show_file_model: false,
		show_file_import: false,
		paginate_url: paginate(@service_groups.assets, params: {}),
		objects_path_export: service_groups_path(format: "csv", service_groups_grid: (params[:service_groups_grid].nil? ? "" : params[:service_groups_grid].to_enum.to_h))
	}
%>

