<%
breadcrumbs = [
	{active: false, href: root_path, text: t('menu.initial_page')},
	{active: false, href: vehicle_types_path, text: VehicleType.model_name.human(count: 2)},
	{active: true, href: '#', text: t('model.list')},
]
%>

<%= 
	render 'common_pages/index',
	attributes: {
		page_title: VehicleType.model_name.human(count: 2),
		url_new_path: new_vehicle_type_path,
		policy_true: policy(VehicleType).new?,
		objects: @vehicle_types,
		objects_path: vehicle_types_path,
		object_name: VehicleType.model_name.human,
		breadcrumbs: breadcrumbs,
		objects_path_export: vehicle_types_path(format: "csv", vehicle_types_grid: (params[:vehicle_types_grid].nil? ? "" : params[:vehicle_types_grid].to_enum.to_h))
	}
%>