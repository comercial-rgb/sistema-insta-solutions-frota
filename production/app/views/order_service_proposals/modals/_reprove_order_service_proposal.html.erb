<!-- Modal -->
<div class="modal fade order-service-proposal-modal" id="reprove_order_service_proposal_<%=order_service_proposal.id%>" tabindex="-1">

    <div class="modal-dialog modal-dialog-scrollable" role="document">
        <div class="modal-content">

            <!-- Cabeçalho -->
            <div class="modal-header">

                <!-- Título -->
                <h5 class="modal-title" id="reprove_order_service_proposal_label_<%=order_service_proposal.id%>">
                    <small class="text-muted"><%= order_service_proposal.code %></small>
                </h5>

                <!-- Botão fechar -->
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <%= form_tag reprove_order_service_proposal_path(id: order_service_proposal.id), method: :post, class: 'form-order-service-proposal-to-validate' do |f| %>
                        <div class="mb-3">
                            <label class="form-label"><%= OrderServiceProposal.human_attribute_name(:reason_reproved) %></label>
                            <%= text_area_tag :reason_reproved, '', class: 'form-control', id: 'reason_reproved_'+order_service_proposal.id.to_s, autofocus: true, autocomplete: 'off' %>
                        </div>
                        <div class="mt-3 text-end">
                            <button class="btn btn-primary btn-sm" type="submit" data-confirm="<%= t('model.confirm_action') %>"><%= t('model.send') %></button>
                        </div>
                <% end %>
            </div>
        </div>
    </div>
</div>
