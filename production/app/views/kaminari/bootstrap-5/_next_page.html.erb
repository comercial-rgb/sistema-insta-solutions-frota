<%# Link to the "Next" page
  - available local variables
    url:           url to the next page
    current_page:  a page object for the currently displayed page
    total_pages:   total number of pages
    per_page:      number of items to fetch per page
    remote:        data-remote
-%>
<li class="page-item <%= current_page.last? ? 'disabled' : '' %>">
  <% if current_page.last? %>
    <a class="page-link" href="#"><%= t('views.pagination.last') %></a>
  <% else %>
    <%= link_to_unless current_page.last?, t('views.pagination.next').html_safe, url, {rel: 'next', remote: remote, class: 'page-link'} %>
  <% end %>
</span>
