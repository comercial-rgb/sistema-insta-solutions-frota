require "rails_helper"

RSpec.describe "commitments/show", type: :view do
  it "renders attributes" do
    skip "Show template expects controller assigns (pagination, totals); use request specs."
  end
end
