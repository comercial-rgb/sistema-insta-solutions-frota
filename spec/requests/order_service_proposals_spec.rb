require 'rails_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to test the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator. If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails. There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.

RSpec.describe "/order_service_proposals", type: :request do
  
  # This should return the minimal set of attributes required to create a valid
  # OrderServiceProposal. As you add validations to OrderServiceProposal, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) {
    skip("Add a hash of attributes valid for your model")
  }

  let(:invalid_attributes) {
    skip("Add a hash of attributes invalid for your model")
  }

  describe "GET /index" do
    it "renders a successful response" do
      OrderServiceProposal.create! valid_attributes
      get order_service_proposals_url
      expect(response).to be_successful
    end
  end

  describe "GET /show" do
    it "renders a successful response" do
      order_service_proposal = OrderServiceProposal.create! valid_attributes
      get order_service_proposal_url(order_service_proposal)
      expect(response).to be_successful
    end
  end

  describe "GET /new" do
    it "renders a successful response" do
      get new_order_service_proposal_url
      expect(response).to be_successful
    end
  end

  describe "GET /edit" do
    it "renders a successful response" do
      order_service_proposal = OrderServiceProposal.create! valid_attributes
      get edit_order_service_proposal_url(order_service_proposal)
      expect(response).to be_successful
    end
  end

  describe "POST /create" do
    context "with valid parameters" do
      it "creates a new OrderServiceProposal" do
        expect {
          post order_service_proposals_url, params: { order_service_proposal: valid_attributes }
        }.to change(OrderServiceProposal, :count).by(1)
      end

      it "redirects to the created order_service_proposal" do
        post order_service_proposals_url, params: { order_service_proposal: valid_attributes }
        expect(response).to redirect_to(order_service_proposal_url(OrderServiceProposal.last))
      end
    end

    context "with invalid parameters" do
      it "does not create a new OrderServiceProposal" do
        expect {
          post order_service_proposals_url, params: { order_service_proposal: invalid_attributes }
        }.to change(OrderServiceProposal, :count).by(0)
      end

    
      it "renders a response with 422 status (i.e. to display the 'new' template)" do
        post order_service_proposals_url, params: { order_service_proposal: invalid_attributes }
        expect(response).to have_http_status(:unprocessable_entity)
      end
    
    end
  end

  describe "PATCH /update" do
    context "with valid parameters" do
      let(:new_attributes) {
        skip("Add a hash of attributes valid for your model")
      }

      it "updates the requested order_service_proposal" do
        order_service_proposal = OrderServiceProposal.create! valid_attributes
        patch order_service_proposal_url(order_service_proposal), params: { order_service_proposal: new_attributes }
        order_service_proposal.reload
        skip("Add assertions for updated state")
      end

      it "redirects to the order_service_proposal" do
        order_service_proposal = OrderServiceProposal.create! valid_attributes
        patch order_service_proposal_url(order_service_proposal), params: { order_service_proposal: new_attributes }
        order_service_proposal.reload
        expect(response).to redirect_to(order_service_proposal_url(order_service_proposal))
      end
    end

    context "with invalid parameters" do
    
      it "renders a response with 422 status (i.e. to display the 'edit' template)" do
        order_service_proposal = OrderServiceProposal.create! valid_attributes
        patch order_service_proposal_url(order_service_proposal), params: { order_service_proposal: invalid_attributes }
        expect(response).to have_http_status(:unprocessable_entity)
      end
    
    end
  end

  describe "DELETE /destroy" do
    it "destroys the requested order_service_proposal" do
      order_service_proposal = OrderServiceProposal.create! valid_attributes
      expect {
        delete order_service_proposal_url(order_service_proposal)
      }.to change(OrderServiceProposal, :count).by(-1)
    end

    it "redirects to the order_service_proposals list" do
      order_service_proposal = OrderServiceProposal.create! valid_attributes
      delete order_service_proposal_url(order_service_proposal)
      expect(response).to redirect_to(order_service_proposals_url)
    end
  end
end
