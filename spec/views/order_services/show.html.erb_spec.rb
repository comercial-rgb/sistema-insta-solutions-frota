require "rails_helper"

RSpec.describe "order_services/show", type: :view do
  it "renders attributes in <p>" do
    skip "View depends on a full OrderService graph; cover with request/system specs."
  end
end
