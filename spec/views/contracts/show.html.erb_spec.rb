require "rails_helper"

RSpec.describe "contracts/show", type: :view do
  it "renders attributes in <p>" do
    skip "Show template expects controller assigns (@available_value, etc.); use request specs."
  end
end
