<div class="container-fluid">
  <div class="row mb-3">
    <div class="col-12">
      <h2><i class="bi bi-car-front"></i> Modelos de Veículos</h2>
    </div>
  </div>

  <div class="row mb-3">
    <div class="col-12">
      <%= link_to new_vehicle_model_path, class: 'btn btn-primary' do %>
        <i class="bi bi-plus-circle"></i> Novo Modelo
      <% end %>
    </div>
  </div>

  <div class="row">
    <div class="col-12">
      <%= datagrid_form_for @grid, url: vehicle_models_path, method: :get %>
      <%= datagrid_table(@grid, class: 'table table-striped table-hover') %>
      <%= paginate @grid.assets %>
    </div>
  </div>
</div>
